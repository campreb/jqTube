(function($){
    $.fn.extend({
       juTube:function(id,options){
           var defaults = {

           };
           var options = $.extend(defaults, options);

           return this.each(function(){
               var obj = $(this);
               
           });
       }
    });
})(jQuery);